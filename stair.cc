#include "stair.h"
	
Stair::Stair (int posx, int posy):Cell{posx, posy, STAIR} {}

//throw
/*
bool Stair::be_visit (Shade& player, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Drow& player, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Vampire& player, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Troll& player, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Goblin& player, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}

//no need
bool Stair::be_visit (Human& enemy, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Dwarf& enemy, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Halfling& enemy, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Orc& enemy, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Elf& enemy, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;

    return false;
}
*bool Stair::be_visit (Dragon& enemy, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
bool Stair::be_visit (Merchant& enemy, Type type){
	if (type == ATTACK) return false;
	if (type == MOVE) return true;
    return false;
}
*/
